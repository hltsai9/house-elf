# house-elf

## Mission Control

An ASCII dashboard to oversee teams of AI agents — see
[`mission_control/`](mission_control/README.md). Pure stdlib, zero installs.

```bash
python -m mission_control demo
```
